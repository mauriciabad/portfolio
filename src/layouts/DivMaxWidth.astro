---
import { cn } from "../lib/cn"

interface Props {
  class?: string
}

const { class: className, ...rest } = Astro.props
---

<div class={cn("mx-auto max-w-4xl", className)} {...rest}>
  <slot />
</div>
